if(${MSVC})
if($ENV{LIBSNDFILE_LOCATION})
set(LIBSNDFILE_PREFIX $ENV{LIBSNDFILE_LOCATION})
message("Libsndfile override successful: $ENV{LIBSNDFILE_LOCATION}")
else()
set(LIBSNDFILE_PREFIX "C:/Program Files (x86)/Mega-Nerd/libsndfile")
message("Libsndfile location override not applied.")
endif()
SET(LIBSNDFILE_INCLUDE_DIR "${LIBSNDFILE_PREFIX}/include")
SET(LIBSNDFILE_LIBRARY_DIR "${LIBSNDFILE_PREFIX}/lib")
SET(LIBSNDFILE_BINARY_DIR "${LIBSNDFILE_PREFIX}/bin")
message("LIBSNDFILE variables:")
message("${LIBSNDFILE_INCLUDE_DIR}")
message("${LIBSNDFILE_LIBRARY_DIR}")
message("${LIBSNDFILE_BINARY_DIR}")

INCLUDE_DIRECTORIES(${LIBSNDFILE_INCLUDE_DIR})
LINK_DIRECTORIES(${LIBSNDFILE_LIBRARY_DIR})
endif()



if(${CMAKE_COMPILER_IS_GNUCC})
SET(libsndfile_name sndfile)
endif()
if(${MSVC})
set(libsndfile_name libsndfile-1)
endif()
